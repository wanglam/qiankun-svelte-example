# qiankun-svelte-example

This project is a example about use svelte in qiankun. Use `yarn` to install all dependencies. Run `yarn start`, then visit [http://localhost:1234/](http://localhost:1234/) to start develop.
